server:
  servlet:
    context-path: /api
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://124.221.136.224:3306/goal?useUnicode=true&characterEncoding=utf-8
    username: root
    password: Senge520...
  web:
    resources:
      static-locations: classpath:/web/
  threads:
    virtual:
      enabled: true
sa-token:
  token-name: token
  # Jwt 密钥
  jwt-secret-key: senge520
  # 不写入 Cookie
  is-read-cookie: false
system:
  cors-enable: true # 是否开启跨域
cos:
  type: tencent
  host: https://${cos.bucket}.cos.ap-beijing.myqcloud.com/
  accessKey: AKIDiRxTovpY5PWO4Zu1xiGdnjQwFNI0Xr0w
  secretKey: rXzEAze8QWUM4bx64pOlz1qU1UkrunU5
  bucket: xiaoyao-diary-1300227149
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl